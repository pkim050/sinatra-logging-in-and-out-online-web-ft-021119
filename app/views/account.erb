<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    #<% binding.pry %>
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= Helpers.current_user(session).username %></h1>
      <h3> Your Current Balance: <%= Helpers.current_user(session).balance %></h3>
      <a href="/logout">Log Out</a>
    <% else %>
      <h3>You must <a href='/'>Log In</a> to view your balance.</h3>
    <% end %>
  </body>
</html>
